{
  "_comment": "Symbol-size ratchet pins (#1155). MEASURED values, never round ceilings. Taken at the bench's own flags (Release = -O3 -DNDEBUG -std=c++23) on the toolchain named below; sizes are compiler-bound, so the gate refuses to compare across toolchains. Re-pin with: python3 bench/symbol_ratchet.py --build bench/build --pins bench/symbol_ratchet.json --emit",
  "toolchain": "g++ (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0",
  "symbols": [
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::graph_t::dispatch_edge_target",
      "bytes": 595,
      "note": "NOT re-pinned by #1448 — byte-identical at 595 B — but its INSTRUCTION STREAM moved 138 -> 148 (+10), and that is recorded here because #1442 is the standing proof that a flat size pin can be blind to a real change. Cause: the edge's ACL fan-in context is borrowed from the shared cold half now, so each of this leg's two `e.caller()` reads is `xor/test/je/mov/mov` (+3) where it used to be two plain loads off the snapshot; `acl_allows` may write through the graph, so the compiler cannot CSE the pair. The remaining +4 is block re-layout around the store call (one `push` reordered) and nop re-padding, which is also why the byte count did not move. Hoisting the context into a local was MEASURED and REJECTED: 145 instructions at 631 B (+36), the wrong side of the trade for a leg whose pin is otherwise unmoved."
    },
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::vertex_t::snapshot_edges",
      "bytes": 843,
      "note": "RE-PINNED 923 -> 843 (-80, 210 -> 199 instructions) by #1448. This is the -O3 WRAPPER, not the copy loop (see the `copy_published` note) — what it inlines is the entry `inline_buf.clear()` / `overflow.clear()` pair, which DESTROYS `edge_view_t`s, and those lost two `std::string` and two `view_t` members. The instruction histogram says exactly that: the inlined refcount-release legs collapse (`xchg` 6 -> 1, `lock` 5 -> 3) and the branches they were guarded by go with them (`je` 18 -> 12, `jmp` 10 -> 6), against two more `call`s where the remaining release is now outlined. A shrink, in the same direction and from the same cause as `copy_published`."
    },
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::vertex_t::copy_published",
      "bytes": 1707,
      "note": "RE-PINNED 3554 -> 1707 (-1847 B, 761 -> 399 instructions) by #1448 — the change this pin was set to watch, arriving as a HALVING. `edge_view_t` stopped owning the cold half's bytes and started holding a `remote_ptr_t` share of it, so the per-entry copy is four words and two refcounts with no branch on the edge shape. What left the loop: the two `tr::detail::try_assign` probe-then-commit windows and their OOM unwind, the two `view_t` clone legs, and the whole per-edge `false` return path (`copy_published` no longer has a per-edge OOM tally — nothing on it can allocate). Instruction histogram: `mov` 301 -> 164, `call` 33 -> 12, `endbr64` 4 -> 2 (two fewer outlined helper entries). Priced on `bench_libtracer fan`, not argued from the disassembly. PREVIOUS NOTE, still the reason this symbol is pinned at all: ADDED by #1442. `snapshot_edges` was already pinned and stayed byte-identical (923 B, +0) across that change — but at -O3 it is only the wrapper: the pinned reader's ACTUAL copy loop is emitted out of line as `copy_published`, and it moved 4157 -> 3554 B (-603, 866 -> 761 instructions) with the pin set blind to it. `try_copy_published`'s source is untouched by #1442; the movement is an inlining-heuristic shuffle triggered by `pub_edge_t::remote` changing type from `std::unique_ptr<pub_remote_t>` to the intrusive `remote_ptr_t` (one `edge_view_t` move constructor outlined, and two `view_t::operator=` legs re-emitted as `segment_ptr_t::reset` calls). It is a SHRINK, and it was priced on the bench rather than argued from the disassembly: fanout-1 and the wide fan-out arms of bench_libtracer were within the same-window A/A null. Pinned here so the next such move is seen at review time instead of by hand."
    },
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::graph_t::fan_out(",
      "bytes": 1893,
      "note": "RE-PINNED 1573 -> 1893 (+320 B) by #1461/#1462 (the receiver-owned STREAM ring). The growth is an INLINE-BUDGET FLIP, not new work on the fan-out path, and it is attributed at instruction level: `std::vector<edge_view_t>::~vector()` — one `call` in the pinned build — is now emitted INLINE at fan_out's cleanup paths, which is the whole delta (5 `operator delete` + 4 `segment_ptr_t::reset()` call sites where there were 1 and 2). It sits on the WIDE-fan-out teardown, below the no-subscriber gate and outside the per-edge loop; the per-edge dispatch body is unchanged, still one indirect callback call plus the two noinline legs, and `dispatch_edge_target` / `dispatch_edge_remote` / `snapshot_edges` / `copy_published` are all byte-identical (+0). The trigger is graph.cpp's own inline estimate moving: `store_value` gained the receiver-ring admission branch and `vertex_t::store` lost its STREAM fork, which re-partitioned the TU's budget — the same class of flip #1250 documents, in the other direction. PRICED ON THE BENCH before landing, as this gate's message demands: `inproc` 64 B / fan 1024 — the fan_out-dominated gated point — best of 3 rounds, interleaved A/B on one host under load avg ~22-39, is 7840 ns p50 against the pinned build's 8100 ns (-3.2%) and 126380 deliv/s against 122303 (+3.3%). Neutral to favourable, so the larger encoding is not paid for in time. PREVIOUS NOTE: RE-PINNED 1613 -> 1573 (-40 B, 364 -> 361 instructions) by #1448. `fan_out` owns the `edge_snapshot_t` stack buffer and the thread-local overflow vector, both of which are arrays of `edge_view_t` — 160 B -> 48 B — so its frame setup and the two `for (const edge_view_t& e : ...)` strides re-encode against smaller displacements and a smaller stack reservation. The per-edge `dispatch_edge` body is unchanged apart from the remote-leg gate becoming `has_remote_leg()`, which is the same one load and one branch it was as `link.empty()` for the in-process edge that dominates a fan-out. The two reclamation bindings are unaffected: this pin measures the DEFAULT build, as the note below insists. PREVIOUS NOTE: RE-PINNED 1474 -> 1613 (+139) by #894 (ADR-0080 reclaim_local). The ONLY pinned symbol this change moves — the other four are byte-identical — and the growth is fully attributed at instruction level: ONE `addl $1,%fs:` on entry, plus a 5-instruction exit epilogue emitted THREE times (the wide TLS-buffer path, the small/stack path, and the .cold unwind clone), plus `nop` re-padding and two cleanup-frame displacement changes that add no instruction. The per-edge dispatch loop body is byte-identical, so the cost is once per fan_out regardless of subscriber count, and the no-subscriber early return is untouched. Priced on the bench: `inproc-batch` fanout-1 (the batch-calibrated bare-notify case) 102 ns -> 102 ns p50, +0.00%, against a same-day A/A null of +/-0.8%. rv32 (-Os, GCC 15.2) is +24 B and +6 instructions on entry; `reclaim_strict` is +0 instructions here. NOT RE-PINNED by #1376 (reclaim_qsbr): this pin measures the DEFAULT binding, which stays at 1613 B (+0) — verified, and backed by object-file identity of graph.cpp.o's whole instruction stream against the pre-#1376 tree. For reference only and explicitly NOT gating: a build that BINDS reclaim_qsbr_t measures 1819 B here (+206), all of it the outermost-bracket announce, the exit epilogues' offline store and the drain early-out, emitted at each of fan_out's exit paths. The per-edge dispatch loop body is instruction-identical between the two bindings, and the announce sits below the no-subscriber gate, so neither a wide fan-out nor an unsubscribed publish pays for it. A QSBR size must never be pinned into this file: the gate runs the default build."
    },
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::graph_t::dispatch_edge_remote",
      "bytes": 318,
      "note": "RE-PINNED 298 -> 318 (+20 B, 74 -> 81 instructions) by #1468 (propagate's FOLD emission mode). The growth is an INLINE-BUDGET FLIP in graph.cpp, not new work on the remote dispatch path, and it is attributed at instruction level: ONE of the two `segment_ptr_t::reset()` CALLS at this function's TEARDOWN is now emitted INLINE — `mov 0x28(%rsp),%rax; test; je; lock subl $0x1,(%rax); je` plus a 3-instruction out-of-line tail (`mov; call destroy_dispatch; jmp`) — where the pinned build had one `lea; call`. That is the entire delta: the delivery body above it (the `read_installed` sink read, the five cold-half wire-field loads, the route setup and the indirect sink call) is INSTRUCTION-IDENTICAL, the `.cold` clone is byte-identical, and the released refcount is the reply scaffolding's, taken AFTER the send. The trigger is the TU's own inline estimate moving: graph.cpp gained `propagate_folded_impl` / `select_sweep`, which call `fan_out`, `bubble_up`, `find_ptr` and `clear_pending` from a second site and re-partition the budget — the same class of flip #1250 documents and dd11bf0d re-pinned for `fan_out`. The FOLD path itself is never on this function's call graph. PRICED ON THE BENCH before landing, as this gate's message demands: `fan-remote` (the dispatch_edge_remote-dominated sweep), best of 3 rounds, A/B interleaved in BOTH orders on one host at 1-min load avg 28-43 (5-min 47-52, 1-27 concurrent `cc1plus`), is neutral to FAVOURABLE at every fan point: deliveries/s +0.6% (fan 1) to +3.0% (fan 1024), p50 -0.0% to -3.4%; the gated wide point, fan 1024, reads 33.84 M/s against the pinned build's 32.86 M/s (+3.0%) and 29970 ns p50 against 30690 (-2.3%). The larger encoding is not paid for in time. PREVIOUS NOTE: RE-PINNED 297 -> 298 (+1 B, 71 -> 74 instructions) by #1448, and the growth is the whole change stated in three instructions. The five wire fields are read off the SHARED cold half now instead of off five members the snapshot had copied, so: (1) one new `mov 0x28(%rbx),%rax` loads the record pointer, after which every field read displaces off %rax at the same instruction count; (2) the sink function pointer is displaced out of %rax into %r8 (`call *%r8`), one extra `mov`; (3) one `nop` of re-padding. Nothing else in the body moved — the two `segment_ptr_t` clones into `remote_delivery_t` and the two `reset`s after the call are instruction-identical. What this +1 B buys is the removal, from `copy_published`, of two `std::string` copies and two `view_t` clones PER REMOTE EDGE PER DELIVERY (-1847 B there). One pointer chase against four heap-touching operations is the trade, and it is taken once per remote delivery on a leg that is already `noinline` and off the wide-fan-out critical path."
    },
    {
      "binary": "bench_libtracer",
      "symbol": "tr::graph::graph_t::find_ptr",
      "bytes": 546
    },
    {
      "binary": "bench_compact_delivery",
      "symbol": "void tr::net::fwd_router_t::route_fwd_forward<tr::wire::grammar::rope_cursor>",
      "bytes": 2231,
      "note": "RE-PINNED 2023 -> 2231 (+208) by #1325 car 4 (RFC-0027 forwarder). The growth is ONE pass-through `std::span` — §6.1's minted reply-leg spelling — threaded into the three `[[gnu::flatten]]` `rebuild_fwd_forward` instantiations this hop inlines. Two cheaper shapes were measured and rejected: deciding the mint inside this hop cost +688 B, and a lazy supplier closure still cost +512 B; hoisting the decision to `route_fwd_ingress` (unpinned, and already switching on `kind`) leaves only the forwarded span. No new work executes on the request path: the `if (!is_reply)` branch that guarded the mount-run assignment became an `is_reply ? ... : ...` select on the same field. NOT priced on the bench yet — RFC-0027 §12.4 (car 5) is the gate that must, including the mandatory four-link reply-spread arm."
    }
  ]
}
