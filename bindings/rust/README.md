# libtracer (Rust binding)

Rust binding for the libtracer reference implementation. Published to crates.io as `libtracer`.

## Status

Stub — not yet implemented.

## Use

```toml
[dependencies]
libtracer = "0.1"
```

## Releasing

Tag the repo with `rust-vX.Y.Z` to trigger publish (see `.github/workflows/publish-crate.yml` once added).
