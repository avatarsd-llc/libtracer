# Documentation build dependencies (Sphinx + MyST Markdown + Furo + Mermaid +
# Breathe for the Doxygen-generated C++ source references). Breathe needs the
# `doxygen` binary at build time (installed in .github/workflows/docs.yml).
sphinx>=7.2,<9
myst-parser>=2.0
furo>=2024.1.29
sphinxcontrib-mermaid>=0.9
breathe>=4.35
