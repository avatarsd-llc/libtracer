# errors/error-registered-code

Bare ERROR carrying the registered-code identity `tr::path::not_found` (code `0x0020`), code-only — the RFC-0002 §C worked example. First child VALUE holds the u16 LE registered code; no detail children.

```
08400600010002002000
```
