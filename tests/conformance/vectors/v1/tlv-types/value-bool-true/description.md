# tlv-types/value-bool-true

A 1-byte VALUE carrying boolean true. type=0x01 VALUE, opt=0x00, length=1 (u16 LE = 01 00), payload=0x01. 5 bytes total; header overhead is 4 bytes.

```
01 00 01 00 01
```
