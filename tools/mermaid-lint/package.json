{
  "name": "libtracer-mermaid-lint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CI syntax gate that parses every docs mermaid block with the same mermaid version the site renders.",
  "license": "Apache-2.0",
  "scripts": {
    "check": "node check-mermaid.mjs"
  },
  "//": "mermaid is pinned to docs/conf.py's mermaid_version (sphinxcontrib-mermaid default). Bump both together so the gate parses exactly what the browser renders.",
  "dependencies": {
    "jsdom": "^26.0.0",
    "mermaid": "11.12.1"
  }
}
